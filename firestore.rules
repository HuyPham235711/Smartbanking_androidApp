rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Chỉ cho phép đọc/ghi nếu user đã đăng nhập
      allow read, write: if request.auth != null;
    }
  }
}